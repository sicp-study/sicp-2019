The cond operator in new-if does a applicative - order evaluation i.e the operands are evaluated first. In this case (sqrt-iter (improve guess x) x))) goes into an infinite loop. The if statement does a normal - order evaluation and hence does not face this issue.
